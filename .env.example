# Public Express API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Set to "true" to use the Express API for news, forms, and admin.
# Default / unset = mock/offline mode (recommended until the API is ready).
NEXT_PUBLIC_USE_API=false
